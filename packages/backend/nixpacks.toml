[phases.setup]
nixPkgs = ["nodejs_20", "yarn"]

[phases.install]
dependsOn = ["setup"]
cmds = ["yarn install --ignore-engines"]

[phases.build]
dependsOn = ["install"]
cmds = ["yarn build"]

[start]
cmd = "yarn start"

[variables]
NIXPACKS_NODE_VERSION = "20.18.0"
CI = "false"
